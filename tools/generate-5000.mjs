import fs from 'node:fs/promises'; await fs.mkdir('data/fixtures',{recursive:true}); const h='name,sku,barcode,regular_price,stock,height_cm,color,status\n'; const r=Array.from({length:5000},(_,i)=>`Тестовий світильник ${i+1},QA-${String(i+1).padStart(5,'0')},482000${String(i+1).padStart(5,'0')},${3699+i%5*500},${i%20},${[50,75,100][i%3]},${['сірий','чорний','зелений'][i%3]},draft`).join('\n'); await fs.writeFile('data/fixtures/ferreta-5000.csv',h+r+'\n'); console.log('5000 SKU generated');
